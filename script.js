let a = "";
let b = "";
let operator = "";

function operate(a, b, operator) {

// create if statements that will complete one of the functions depending on operator
    if (operator === "add") {

    }

}


function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    if (b == 0) {
        return "ERR";
    } else {
    return a/b;
    }
}